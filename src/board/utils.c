/**
 * utils.c
 * 
 * Implementations of functions declared in utils.h.
 * 
 * @author Dean Biskup and Mitalee Bharadwaj
 * University of Illinois, ADSL (ECE 395) Fall 2019
 */

#include "utils.h"

int abs(int x) {
  return x < 0 ? -x : x;
}
